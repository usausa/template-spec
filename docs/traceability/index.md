# トレーサビリティ索引

`REQ → DES → ADR → test → code` の対応表。**`/trace` で検査・更新**する(手で厳密に保つ必要はない)。

| REQ | DES | ADR | テスト | コード | 備考 |
|-----|-----|-----|--------|--------|------|
| —   | —   | ADR-0001 | — | — | まだ機能未追加。最初の REQ を追加したらここに行を足す |

## /trace が検出するもの
- `related` の ID が実在しない(リンク切れ)
- どこからも参照されない孤児 REQ/DES
- `status: superseded` なのに後継 ADR が無い/実在しない
- 公開 API を変えた形跡があるのに ADR が無い(決定漏れ)
